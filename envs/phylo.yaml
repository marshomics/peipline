channels:
  - conda-forge
  - bioconda
  - nodefaults   # never fall back to `defaults`/`anaconda` (licensing + not on the cluster)
dependencies:
  - iqtree=2.3.6
  - trimal=1.4.1
  - seqkit=2.8.2
  - mmseqs2=15.6f452
  - hmmer=3.4
  - python=3.11
  - pyyaml=6.0
