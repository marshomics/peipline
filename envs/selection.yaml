channels:
  - conda-forge
  - bioconda
  - nodefaults   # never fall back to `defaults`/`anaconda` (licensing + not on the cluster)
dependencies:
  - hyphy=2.5.62
  - iqtree=2.3.6
  - python=3.11
  - numpy=1.26
  - pandas=2.2
  - scipy=1.13
  - statsmodels=0.14
  - matplotlib-base=3.8
  - pyyaml=6.0
